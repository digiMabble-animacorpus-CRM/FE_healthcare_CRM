export type FamilyMember = {
  name: string;
  relation: string;
  age: number;
};

export type AppointmentDetails = {
  date: string;
  time?: string;
  purpose: string;
  status: string;
};

export type CustomerType = {
  title: string;
  Property: number;
  icon: string;
  count: string;
  progress: number;
  variant: string;
};
